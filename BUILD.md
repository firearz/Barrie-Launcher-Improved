# 🔨 Build Instructions

This guide explains how to build the Barrie Launcher as a standalone `.exe` using PyInstaller.

---

## ✅ Requirements

- Python 3.10 or later
- Pip
- Modules:
  - `PySide6`
  - `psutil`
  - `minecraft-launcher-lib`
  - `pyinstaller`

Install dependencies:

```bash
pip install -r requirements.txt
```
or manually by 

```pip install PySide6 psutil minecraft-launcher-lib pyinstaller```
